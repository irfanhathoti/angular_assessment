import { SearchLoactionPipe } from './search-loaction.pipe';

describe('SearchLoactionPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchLoactionPipe();
    expect(pipe).toBeTruthy();
  });
});
